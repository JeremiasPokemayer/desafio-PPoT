{
  "name": "desafio-PPoT",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.13.3"
  },
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir build --public-url /desafio-PPoT",
    "deploy": "gh-pages -d build"
  }
}
